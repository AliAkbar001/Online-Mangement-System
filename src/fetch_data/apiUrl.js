const port = 5001;
const localhost = `localhost:${port}`;
const online = 'www.aliakbar.pro';
export const productImage = `http://${localhost}/api/products/image`
export const productURL = `http://${localhost}/api/products`;